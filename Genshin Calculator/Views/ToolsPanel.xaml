<UserControl x:Class="Genshin_Calculator.Views.ToolsPanel"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ToolsPanelViewModel, IsDesignTimeCreatable=False}"
             xmlns:vm="clr-namespace:Genshin_Calculator.ViewModels">
    <!-- d:DataContext="{d:DesignInstance Type=vm:ToolsPanelViewModel, IsDesignTimeCreatable=False}" -->

    <Grid>
        <WrapPanel HorizontalAlignment="Center" Margin="10">
            <Button Content="Add Character"
                    Width="150"
                    Height="30"
                    Command="{Binding TogglePopupCommand}" />

            <Button Content="Manage Inventory"
                    Width="150"
                    Height="30"
                    Command="{Binding ManageInventoryCommand}" />

            <Button Content="Manage Priority"
                    Width="150"
                    Height="30"
                    Command="{Binding ManagePriorityCommand}" />
        </WrapPanel>

        <Popup IsOpen="{Binding IsPopupOpen, Mode=TwoWay}"
               Placement="Center"
               StaysOpen="False">
            <Border Width="300" Height="200"
                    Background="#FF1E1E2A"
                    CornerRadius="10"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    Padding="10">

                <StackPanel>
                    <TextBlock Text="Select a character"
                       FontWeight="Bold"
                       FontSize="16"
                       Foreground="White"
                       Margin="0,0,0,10" />

                    <ListView ItemsSource="{Binding AvailableCharacters}"
                          SelectedItem="{Binding SelectedCharacter, Mode=TwoWay}">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Center" />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="5" BorderBrush="Transparent" BorderThickness="2">
                                    <Image Source="{Binding ImagePath}"
                                   Width="64"
                                   Height="64"
                                   Cursor="Hand">
                                        <Image.InputBindings>
                                            <MouseBinding Command="{Binding DataContext.SelectCharacterCommand, RelativeSource={RelativeSource AncestorType=Popup}}"
                                                  CommandParameter="{Binding}" MouseAction="LeftClick" />
                                        </Image.InputBindings>
                                    </Image>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Button Content="Confirm"
                            Width="100"
                            Margin="0,10,0,0"
                            HorizontalAlignment="Right"
                            Command="{Binding ConfirmAddCharacterCommand}" />
                </StackPanel>
            </Border>
        </Popup>
    </Grid>

</UserControl>