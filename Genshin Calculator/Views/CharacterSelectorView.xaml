<Window x:Class="Genshin_Calculator.Views.CharacterSelectorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Background="#FF1E1E2A"
        Title="Select Character"
        xmlns:vm="clr-namespace:Genshin_Calculator.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        xmlns:h="clr-namespace:Genshin_Calculator.Helpers"
        d:DataContext="{d:DesignInstance Type=vm:CharacterSelectorViewModel, IsDesignTimeCreatable=False}">

    <Border Padding="15" Background="#1E1E2A" CornerRadius="12">
        <Grid Width="650">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <!-- Поиск и фильтры -->

            <StackPanel Grid.Row="0"
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            Margin="0 0 0 10"
            VerticalAlignment="Center">
                <TextBox Width="200" h:TextBoxHelper.Placeholder="Search..."
             Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
             Foreground="White"
             Background="#2A2A3C"
             Padding="5"
             Margin="0 0 10 0" />
                <ToggleButton IsChecked="{Binding IsSortByRarity}"
                  Style="{StaticResource SortToggleButtonStyle}" />
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal"
                         Margin="0 10 0 10" HorizontalAlignment="Center">

                <ItemsControl ItemsSource="{Binding ElementTypes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Command="{Binding DataContext.ToggleElementCommand,
                                                RelativeSource={RelativeSource AncestorType=Window}}"
                              CommandParameter="{Binding}"
                              Margin="4"
                              Width="36" Height="36"
                              Background="Transparent"
                              BorderThickness="0"
                              Cursor="Hand">

                                <ToggleButton.IsChecked>
                                    <MultiBinding Converter="{StaticResource ContainsConverter}" Mode="OneWay">
                                        <Binding Path="DataContext.SelectedElements"
                                     RelativeSource="{RelativeSource AncestorType=Window}" />
                                        <Binding Path="." />
                                    </MultiBinding>
                                </ToggleButton.IsChecked>

                                <Image Source="{Binding Converter={StaticResource ElementToIconConverter}}"
                           Width="32" Height="32" />

                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="bd"
                                                CornerRadius="6"
                                                Background="{TemplateBinding Background}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                    </Border>

                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="bd" Property="Background" Value="#20FFFFFF" />
                                                        </Trigger>

                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="bd" Property="Background" Value="#40FFFFFF" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>

                            </ToggleButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Rectangle Width="1" Height="32" Fill="#66FFFFFF" Margin="12 0" />

                <ItemsControl ItemsSource="{Binding WeaponTypes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Command="{Binding DataContext.ToggleWeaponCommand,
                                                RelativeSource={RelativeSource AncestorType=Window}}"
                              CommandParameter="{Binding}"
                              Margin="4"
                              Width="36" Height="36"
                              Background="Transparent"
                              BorderThickness="0"
                              Cursor="Hand">

                                <ToggleButton.IsChecked>
                                    <MultiBinding Converter="{StaticResource ContainsConverter}" Mode="OneWay">
                                        <Binding Path="DataContext.SelectedWeapons"
                                     RelativeSource="{RelativeSource AncestorType=Window}" />
                                        <Binding Path="." />
                                    </MultiBinding>
                                </ToggleButton.IsChecked>

                                <Image Source="{Binding Converter={StaticResource WeaponToIconConverter}}"
                           Width="32" Height="32" />

                            </ToggleButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>

            <!-- Сетка персонажей -->
            <ScrollViewer Grid.Row="2" Height="400">

                <ItemsControl ItemsSource="{Binding FilteredCharacters}" HorizontalAlignment="Center">
                    <ItemsControl.ItemContainerStyle>
                        <Style>
                            <Setter Property="FrameworkElement.Margin" Value="5" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <ItemsControl.Resources>
                        <Style TargetType="Button">
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Cursor" Value="Hand" />
                            <Setter Property="Focusable" Value="False" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ItemsControl.Resources>

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.SelectCharacterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}">

                                <StackPanel>
                                    <Grid Width="96" Height="96" ClipToBounds="True">
                                        <Border Background="{Binding Assets.Rarity, Converter={StaticResource RarityToBrushConverter}}" CornerRadius="10" />
                                        <Border CornerRadius="10" ClipToBounds="True">
                                            <Image Source="{Binding Assets.ImagePath}"
                                                Stretch="UniformToFill" />
                                        </Border>
                                    </Grid>

                                    <TextBlock Text="{Binding Name}"
                                                Foreground="White"
                                                FontSize="12"
                                                FontWeight="Bold"
                                                HorizontalAlignment="Center"
                                                TextAlignment="Center"
                                                Margin="0,0,0,4"
                                                MaxWidth="96" />
                                </StackPanel>

                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>

            </ScrollViewer>

        </Grid>
    </Border>
</Window>