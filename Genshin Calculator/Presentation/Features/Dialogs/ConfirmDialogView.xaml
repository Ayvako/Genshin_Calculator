<Window x:Class="Genshin_Calculator.Presentation.Features.Dialogs.ConfirmDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Width="400" Height="200" WindowStartupLocation="CenterOwner"
        xmlns:controls="clr-namespace:Genshin_Calculator.Presentation.Controls">

    <Border CornerRadius="12" BorderBrush="#3B4255" BorderThickness="2">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#1C2036" Offset="0" />
                <GradientStop Color="#111322" Offset="1" />
            </LinearGradientBrush>
        </Border.Background>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Title}" Foreground="#E8D07A"
                       FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" />

            <TextBlock Grid.Row="1" Text="{Binding Message}" Foreground="#EAE6D8"
                       FontSize="14" TextWrapping="Wrap" TextAlignment="Center"
                       VerticalAlignment="Center" Margin="0,10" />

            <Border Grid.Row="2" Padding="16,12,16,16">
                <controls:DialogButtonsControl />
            </Border>
        </Grid>
    </Border>
</Window>