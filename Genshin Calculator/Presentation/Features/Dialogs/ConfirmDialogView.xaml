<Window
    x:Class="Genshin_Calculator.Presentation.Features.Dialogs.ConfirmDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Genshin_Calculator.Presentation.Controls"
    Width="400"
    Height="200"
    AllowsTransparency="True"
    Background="Transparent"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None">

    <Border
        BorderBrush="#3B4255"
        BorderThickness="2"
        CornerRadius="12">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Offset="0" Color="#1C2036" />
                <GradientStop Offset="1" Color="#111322" />
            </LinearGradientBrush>
        </Border.Background>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                HorizontalAlignment="Center"
                FontSize="18"
                FontWeight="Bold"
                Foreground="#E8D07A"
                Text="{Binding Title}" />

            <TextBlock
                Grid.Row="1"
                Margin="0,10"
                VerticalAlignment="Center"
                FontSize="14"
                Foreground="#EAE6D8"
                Text="{Binding Message}"
                TextAlignment="Center"
                TextWrapping="Wrap" />

            <Border Grid.Row="2" Padding="16,12,16,16">
                <controls:DialogButtonsControl />
            </Border>
        </Grid>
    </Border>
</Window>