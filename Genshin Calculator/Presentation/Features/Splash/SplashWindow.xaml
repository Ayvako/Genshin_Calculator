<Window x:Class="Genshin_Calculator.Presentation.Features.Splash.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading" Height="200" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent">
    <Border Background="#1a1a2e" CornerRadius="12">
        <StackPanel VerticalAlignment="Center" Margin="30">
            <TextBlock Text="Genshin Calculator" Foreground="White"
                       FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />
            <TextBlock x:Name="StatusText" Text="Starting..." Foreground="#aaaaaa"
                       FontSize="13" HorizontalAlignment="Center" Margin="0,12,0,8" />
            <ProgressBar x:Name="ProgressBar" Height="6" Minimum="0" Maximum="100"
                         Background="#333" Foreground="#c0a060" BorderThickness="0" />
        </StackPanel>
    </Border>
</Window>